services:
    tailscale:
        name: Tailscale
        commands:
          start: sudo tailscale up --accept-routes --auth-key $TAILSCALE­_AUTHKEY
          stop: sudo tailscale down
        triggeredBy:
            - postEnvironmentStart
